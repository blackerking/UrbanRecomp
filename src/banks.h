#ifndef BANKS_H
#define BANKS_H

#include "types.h"

/* v2 ABI: every recompiled function takes `CpuState *cpu` and mutates
 * register/flag state in place. No per-bank aggregate typedefs are needed;
 * generated code addresses memory directly. */

#endif // BANKS_H
