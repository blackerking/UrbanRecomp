[General]
; Not yet read by src/main.c -- placeholder following the snesrecomp game-repo
; convention (see MegaManXSNESRecomp/config.ini) so the file exists in the
; expected shape as config parsing is wired up.
Autosave = 0

[Graphics]
WindowScale = 3

[Sound]
EnableAudio = 1
AudioFreq = 32040
